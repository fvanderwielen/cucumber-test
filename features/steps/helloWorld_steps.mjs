const { Before, Given, When, Then } = require('cucumber');
 
When('we request the politie homepage', function () {
	// Write code here that turns the phrase above into concrete actions
	return 'pending';
});

 
Then('we should see something', function () {
	// Write code here that turns the phrase above into concrete actions
	return 'pending';
});
